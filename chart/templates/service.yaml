apiVersion: v1
kind: Service
metadata:
  name: {{ include "nats-gatekeeper.serviceName" . }}
  labels:
    {{- include "nats-gatekeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $port := .Values.service.ports }}
    - port: {{ $port }}
      protocol: TCP
      name: {{ $name }}
    {{- end }}
  selector:
    {{- include "nats-gatekeeper.selectorLabels" . | nindent 4 }}
